

function takeOrder(order1, deliveryOrders) {
  if(deliveryOrders.length < 3){
  return deliveryOrders.push(order1)
  } else {
    return
  }
}

function refundOrder(1657, deliveryOrders){
  return deliveryOrders.remove(2893)
}

module.exports = {
  takeOrder,
  refundOrder,
  // listItems,
  // searchOrder
}